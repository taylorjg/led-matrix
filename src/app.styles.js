import styled from "@emotion/styled";

export const StyledLedMatrixWrapper = styled.div`
  height: 20vh;
`;

export const StyledFullscreenExitIcon = styled.div`
  position: fixed;
  bottom: 0.5rem;
  left: 0.5rem;
  cursor: pointer;
`;
